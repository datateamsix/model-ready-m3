# Backend JSON Schema Exports

Populated by the backend track once `docs/contracts/BACKEND_REQUESTS.md`'s `REQ-001` (contract
schema export) is implemented. Empty as of this commit — see `../README.md` for the intended
pipeline.
