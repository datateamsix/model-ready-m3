import { LogIn } from "lucide-react";
import { RouteStub } from "@/components/prem3/route-stub";

export default function Page() {
  return (
    <RouteStub
      icon={LogIn}
      title="Sign in"
      description="Branded Clerk sign-in lands in Mission 2 prompt M2-06."
    />
  );
}
