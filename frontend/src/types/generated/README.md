# Generated Backend Types

Populated by `npm run contracts:generate` from `../../../../contracts/openapi.yaml` (Mission 2
`M2-02`). **Do not hand-edit files in this directory** — every generated file carries a
do-not-edit header once real generation is wired up; changes belong in the backend's OpenAPI
source, not here.

**Status as of 2026-08-17: empty.** `contracts/openapi.yaml` doesn't exist yet (blocked on
`docs/contracts/BACKEND_REQUESTS.md`'s `REQ-002`), so there is nothing to generate. Mission 1's
hand-mirrored contract types (`frontend/src/types/response.ts`, `run.ts`, `mel.ts`,
`domain-view.ts`, `intelligence.ts`) remain exactly where they are, hand-verified against the
real Python source per `frontend/CLAUDE.md`'s rule, until a real generated replacement exists to
migrate them to. They are not moved here prematurely.
