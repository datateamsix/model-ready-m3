# Expected trajectories

Store versioned expected tool/state trajectories used by ADK evaluation. Trajectory checks are advisory to deterministic outcome checks and cannot override a failing readiness or publish-parity validator.
