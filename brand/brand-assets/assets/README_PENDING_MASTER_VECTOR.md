# Final Artwork Placeholder

This folder intentionally does **not** contain guessed or auto-traced production SVGs.

Reason: the approved mark has custom perspective, spacing, fragment placement, and color distribution. Shipping a mechanically reconstructed SVG before manual fidelity review would violate the brand decision.

Populate this folder only with vector masters that pass `spec/QA_CHECKLIST.md` and match the filenames in `manifest/asset-manifest.json`.
