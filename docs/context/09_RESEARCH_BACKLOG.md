# Research Backlog

## P0 — Meridian executable rules
- Build full rule catalog from current Meridian pre-modeling docs.
- Enumerate exact loader constraints.
- Map geo / national / R&F variants.
- Determine easiest machine-level "Meridian-compatible" validation method.
- Test MMM Unified Schema package and serialization.

## P0 — Provider registry seed
For each selected provider:
- official export/API docs;
- common report types;
- field names;
- data types;
- grain;
- date handling;
- geo availability;
- attribution/window caveats;
- export limits;
- raw summable metrics;
- known transformations.

Initial:
1. Google Ads
2. Meta Ads
3. GA4
4. Shopify

Stretch:
5. LinkedIn Ads
6. TikTok Ads
7. Microsoft Ads
8. Salesforce/HubSpot
9. Search Console

## P0 — Synthetic dataset
- define Music Center commercial history;
- generate 2–3 years weekly KPI;
- generate channel spend/exposure;
- generate campaign-level exports;
- seed known defects;
- generate expected-answer manifest.

## P0 — BigQuery model contract
- define versioned dataset/table/view naming convention;
- define model-input schema contract;
- define channel-mapping table/JSON;
- define provenance and transformation-manifest storage;
- implement row/schema/fingerprint parity checks;
- generate Meridian input mapping/config from the published artifact;
- verify easiest direct BigQuery-to-Meridian loading/handoff path.

## P0 — PreM3 naming and receipts
- keep user-facing identity as PreM3;
- define `PreM3 Learning Receipt` UI/data schema;
- define `Experience Applied` receipt;
- ensure demo metrics are populated from actual run evidence.

## Upcoming priorities (do not execute in the rebrand)

Sequence:
1. PreM3 MMM boot context — complete
2. computational readiness tools — complete
3. semantic readiness interview — complete
4. MEL Episode Core — implemented; first real learning cycle not proven
5. Dataset B Stride & Field learning-evidence fixture — generated; not a promoted lesson
6. Ambient Taskmaster
7. Experience Applied proof on sealed Dataset C — Dataset C sealed; application not yet run
8. held-out Dataset C — sealed hospitality Summit & Pine assignment at `datasets/summit_and_pine/dataset_c/`

## P1 — Experiential learning
- test Vertex AI Memory Bank;
- define memory consolidation behavior;
- evaluate ADK eval file format;
- test BigQuery Agent Analytics plugin;
- prove lesson retrieval changes tool path;
- define lesson promotion threshold.

## P1 — Additional Google AI model bonus
Investigate only legitimate product uses:
- Gemma for low-cost schema classification;
- embeddings for schema/registry retrieval;
- other qualifying Google AI models.

Confirm competition bonus eligibility before implementation.

## P1 — Cloud proof
- Cloud Run service;
- log trace;
- BigQuery tables;
- Memory Bank;
- screenshot/video-friendly evidence.

## P1 — Meridian execution handoff
- research official Cortex for Meridian BigQuery/config conventions;
- generate execution-ready config package;
- evaluate Cloud Workflows + Colab Enterprise path;
- implement approval boundary;
- prove that M3 can leave a validated model one explicit approval away from execution.

## P2 — Full model run
Evaluate whether running a small Meridian model after readiness adds enough judge value to justify runtime/demo risk.

Default: PreM3 must publish a validated BigQuery model artifact and complete Meridian handoff contract. Full model execution remains optional proof.
