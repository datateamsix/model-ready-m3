# Integration tests

Use this directory for GCS, BigQuery, ADK, and Cloud Run boundary tests.

Integration tests must use run-scoped/versioned resources and must never overwrite an unrelated production model contract.
